#include <unistd.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *word = "_putchar"
		for (int i = 0; i <= strlrn(word); i++)
		{
			_putchar(word[i]);
		}
	return (0);
}

